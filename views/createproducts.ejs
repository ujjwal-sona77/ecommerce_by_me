<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Product</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <% if(success.length>0){ %>
        <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
            <span class="inline-block mt-1 mb-1 text-white">
                <%= success %>
            </span>
        </div>
    <% } %>

    <div class="min-h-screen flex flex-col">
        <div class="container px-10 py-20 flex flex-grow">
            <div class="w-[25%] flex h-screen flex-col items-start">
                <div class="flex flex-col">
                    <a class="block w-fit mb-2 hover:text-blue-500" href="/owner/allproducts">All Products</a>
                    <a class="block w-fit mb-2 hover:text-blue-500" href="/owner/createproduct">Create new product</a>
                </div>
            </div>
            <main class="w-3/4 bg-white p-8 shadow-lg rounded-lg ml-4">
                <h2 class="text-2xl font-bold mb-6 text-gray-800">Create New Product</h2>
                <form autocomplete="off" action="/owner/createproducts" method="post" enctype="multipart/form-data">
                    <div class="mb-8">
                        <h3 class="text-lg font-semibold mb-4 text-gray-700">Product Details</h3>
                        <div class="mb-6">
                            <label class="block mb-2 font-medium text-gray-600">Product Image</label>
                            <input name="image" type="file" accept="image/*" class="py-2 px-4 rounded border border-gray-300 w-full" required>
                        </div>
                        <div class="grid grid-cols-2 gap-6">
                            <div>
                                <label class="block mb-2 font-medium text-gray-600">Product Name</label>
                                <input name="name" type="text" placeholder="Enter product name" class="border border-gray-300 p-3 rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                            </div>
                            <div>
                                <label class="block mb-2 font-medium text-gray-600">Product Price</label>
                                <input name="price" type="number" step="0.01" placeholder="Enter price" class="border border-gray-300 p-3 rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                            </div>
                            <div>
                                <label class="block mb-2 font-medium text-gray-600">Discount Price</label>
                                <input name="discount" type="number" step="0.01" placeholder="Enter discount price" class="border border-gray-300 p-3 rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-500">
                            </div>
                        </div>
                    </div>
                    <button type="submit" class="px-6 py-3 rounded-lg bg-blue-500 text-white font-medium hover:bg-blue-600 transition duration-200">
                        Create Product
                    </button>
                </form>
            </main>
        </div>
    </div>
</body>

</html>
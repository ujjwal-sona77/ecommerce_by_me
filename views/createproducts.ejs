<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Product</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <% if(success.length>0){ %>
        <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
            <span class="inline-block mt-1 mb-1 text-white">
                <%= success %>
            </span>
        </div>
    <% } %>

    <div class="min-h-screen flex flex-col">
        <div class="container px-4 sm:px-6 lg:px-10 py-8 sm:py-12 lg:py-20 flex flex-col lg:flex-row flex-grow">
            <!-- Sidebar -->
            <div class="w-full lg:w-[25%] mb-8 lg:mb-0">
                <div class="flex flex-row lg:flex-col space-x-4 lg:space-x-0">
                    <a class="block w-fit mb-0 lg:mb-2 hover:text-blue-500" href="/owner/allproducts">All Products</a>
                    <a class="block w-fit mb-0 lg:mb-2 hover:text-blue-500" href="/owner/createproduct">Create new product</a>
                </div>
            </div>

            <!-- Main Content -->
            <main class="w-full lg:w-3/4 bg-white p-4 sm:p-6 lg:p-8 shadow-lg rounded-lg lg:ml-4">
                <h2 class="text-xl sm:text-2xl font-bold mb-4 sm:mb-6 text-gray-800">Create New Product</h2>
                <form autocomplete="off" action="/owner/createproducts" method="post" enctype="multipart/form-data">
                    <div class="mb-6 sm:mb-8">
                        <h3 class="text-base sm:text-lg font-semibold mb-3 sm:mb-4 text-gray-700">Product Details</h3>
                        <div class="mb-4 sm:mb-6">
                            <label class="block mb-2 font-medium text-gray-600">Product Image</label>
                            <input name="image" type="file" accept="image/*" class="py-2 px-4 rounded border border-gray-300 w-full text-sm sm:text-base" required>
                        </div>
                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 sm:gap-6">
                            <div>
                                <label class="block mb-2 font-medium text-gray-600">Product Name</label>
                                <input name="name" type="text" placeholder="Enter product name" class="border border-gray-300 p-2 sm:p-3 rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm sm:text-base" required>
                            </div>
                            <div>
                                <label class="block mb-2 font-medium text-gray-600">Product Price</label>
                                <input name="price" type="number" step="0.01" placeholder="Enter price" class="border border-gray-300 p-2 sm:p-3 rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm sm:text-base" required>
                            </div>
                            <div>
                                <label class="block mb-2 font-medium text-gray-600">Discount Price</label>
                                <input name="discount" type="number" step="0.01" placeholder="Enter discount price" class="border border-gray-300 p-2 sm:p-3 rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm sm:text-base">
                            </div>
                        </div>
                    </div>
                    <button type="submit" class="w-full sm:w-auto px-4 sm:px-6 py-2 sm:py-3 rounded-lg bg-blue-500 text-white font-medium hover:bg-blue-600 transition duration-200 text-sm sm:text-base">
                        Create Product
                    </button>
                </form>
            </main>
        </div>
    </div>
</body>

</html>
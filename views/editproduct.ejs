<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <% if(success.length>0){ %>
        <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
            <span class="inline-block mt-1 mb-1 text-white">
                <%= success %>
            </span>
        </div>
    <% } %>

    <div class="min-h-screen flex flex-col">
        <div class="container px-10 py-20 flex flex-grow">
            <div class="w-[25%] flex h-screen flex-col items-start">
                <div class="flex flex-col">
                    <a class="block w-fit mb-2 hover:text-blue-500" href="/owner/allproducts">All Products</a>
                    <a class="block w-fit mb-2 text-blue-500 font-medium" href="#">Edit Product</a>
                </div>
            </div>
            <main class="w-3/4 bg-white p-8 shadow ml-4 rounded-lg">   
                <h2 class="text-2xl font-bold mb-6">Edit Product</h2>
                <form autocomplete="off" action="/owner/edit/products/<%= product._id %>" method="post" enctype="multipart/form-data">
                    <div class="mb-8">
                        <h3 class="text-lg font-semibold mb-4">Product Details</h3>
                        <div class="mb-6">
                            <label class="block mb-2 font-medium text-gray-700">Product Image</label>
                            <input name="image" type="file" class="py-2 px-4 rounded border w-full" accept="image/*">
                            <% if(product.image) { %>
                                <img src="data:image/jpeg;base64,<%= product.image.toString('base64') %>" alt="Current product image" class="mt-2 h-32 object-contain">
                            <% } %>
                        </div>
                        <div class="grid grid-cols-2 gap-6">
                            <div>
                                <label class="block mb-2 font-medium text-gray-700">Product Name</label>
                                <input name="name" type="text" placeholder="Enter product name" value="<%= product.name %>"
                                    class="border p-3 rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-500">
                            </div>
                            <div>
                                <label class="block mb-2 font-medium text-gray-700">Product Price</label>
                                <input name="price" type="number" step="0.01" placeholder="Enter price" value="<%= product.price %>"
                                    class="border p-3 rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-500">
                            </div>
                            <div>
                                <label class="block mb-2 font-medium text-gray-700">Discount Price</label>
                                <input name="discount" type="number" step="0.01" placeholder="Enter discount price" value="<%= product.discount %>"
                                    class="border p-3 rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-500">
                            </div>
                        </div>
                    </div>

                    <div class="mb-6">
                        <h3 class="text-lg font-semibold mb-4">Panel Details</h3>
                        <div class="grid grid-cols-2 gap-6">
                            <div>
                                <label class="block mb-2 font-medium text-gray-700">Background Color</label>
                                <input name="bgcolor" type="color" value="<%= product.bgcolor %>"
                                    class="h-10 w-full rounded border">
                            </div>
                            <div>
                                <label class="block mb-2 font-medium text-gray-700">Panel Color</label>
                                <input name="panelcolor" type="color" value="<%= product.panelcolor %>"
                                    class="h-10 w-full rounded border">
                            </div>
                            <div>
                                <label class="block mb-2 font-medium text-gray-700">Text Color</label>
                                <input name="textcolor" type="color" value="<%= product.textcolor %>"
                                    class="h-10 w-full rounded border">
                            </div>
                        </div>
                    </div>
                    <div class="flex justify-end gap-4">
                        <a href="/owner/allproducts" class="px-6 py-2 rounded bg-gray-100 hover:bg-gray-200 text-gray-700">Cancel</a>
                        <button type="submit" class="px-6 py-2 rounded bg-blue-500 hover:bg-blue-600 text-white">
                            Save Changes
                        </button>
                    </div>
                </form>
            </main>
        </div>
    </div>
</body>

</html>
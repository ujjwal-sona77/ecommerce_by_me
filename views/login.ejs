<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | E-Commerce</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    html,
    body {
        width: 100%;
        height: 100%;
    }
</style>

<body>
    <% if(error.length>0){ %>
        <div class="fixed top-5 left-1/2 -translate-x-1/2 z-50 w-11/12 max-w-sm">
            <div class="p-3 rounded-md bg-red-500">
                <span class="block text-sm sm:text-base text-white text-center">
                    <%= error %>
                </span>
            </div>
        </div>
    <% } %>
    
    <main class="bg-zinc-800 min-h-screen">
        <nav class="flex justify-between items-center p-4 sm:p-6 bg-zinc-800 text-white">
            <h1 class="text-xl sm:text-2xl md:text-3xl font-bold" style="font-family: 'Poppins', sans-serif;">E-Commerce</h1>
        </nav>

        <div class="px-4 py-8 sm:py-12">
            <form action="/users/loginuser" method="post" class="max-w-sm mx-auto bg-zinc-900 p-6 sm:p-8 rounded-lg shadow-lg">
                <div class="mb-6">
                    <label for="email" class="block mb-2 text-sm font-medium text-white">
                        Your email
                    </label>
                    <input type="email" name="email" id="email"
                        class="w-full p-3 text-sm rounded-lg bg-gray-700 border border-gray-600 placeholder-gray-400 text-white focus:ring-blue-500 focus:border-blue-500"
                        placeholder="name@example.com" required />
                </div>

                <div class="mb-6">
                    <label for="password" class="block mb-2 text-sm font-medium text-white">
                        Your password
                    </label>
                    <input type="password" name="password" id="password"
                        class="w-full p-3 text-sm rounded-lg bg-gray-700 border border-gray-600 placeholder-gray-400 text-white focus:ring-blue-500 focus:border-blue-500"
                        required />
                </div>

                <div class="flex items-center mb-6">
                    <div class="flex items-center">
                        <input id="remember" type="checkbox"
                            class="w-4 h-4 rounded bg-gray-700 border-gray-600 focus:ring-blue-600 ring-offset-gray-800"
                            required />
                        <label for="remember" class="ml-2 text-sm text-gray-300">
                            Remember me
                        </label>
                    </div>
                </div>

                <button type="submit"
                    class="w-full p-3 text-sm font-medium text-white bg-blue-600 rounded-lg hover:bg-blue-700 focus:ring-4 focus:ring-blue-800 transition-colors">
                    Login
                </button>

                <div class="mt-6 text-center">
                    <span class="text-sm text-gray-400">Don't have an account?</span>
                    <a href="/" class="text-sm text-blue-500 hover:text-blue-400 ml-1 transition-colors">Sign up</a>
                </div>
            </form>
        </div>
    </main>
</body>

</html>